{
  "private": true,
  "scripts": {
    "build": "amu compile --target wasm32-browser engine r2-engine ipfs-engine",
    "check": "amu compile --target wasm32-browser engine",
    "deploy": "npm run build && wrangler deploy",
    "dev": "npm run build && wrangler dev",
    "migrate:remote": "wrangler d1 migrations apply DB --remote",
    "verify:remote": "node scripts/verify.mjs",
    "benchmark": "KOTOBASE_BENCH=1 node scripts/verify.mjs",
    "slo:qualify": "node scripts/slo-qualify.mjs",
    "slo:r2-qualify": "node scripts/r2-qualify.mjs",
    "test:r2-backfill": "node --test test/r2-backfill-worker.test.mjs",
    "test:core": "node --test test/kotobase-core.test.mjs",
    "test:r2-authority": "amu compile --target wasm32-browser r2-contract-test && node dist/r2-contract-test.js",
    "test:ipfs-authority": "amu compile --target wasm32-browser ipfs-contract-test && node dist/ipfs-contract-test.js",
    "test": "node --test test/successor.test.mjs && amu compile --target wasm32-browser contract-test && node dist/contract-test.js && npm run test:core && npm run test:r2-backfill && npm run test:r2-authority && npm run test:ipfs-authority"
  },
  "devDependencies": {
    "@ipld/dag-cbor": "^9.2.1",
    "@noble/curves": "^1.7.0",
    "miniflare": "^4.20260722.1",
    "multiformats": "^13.3.1",
    "shadow-cljs": "3.4.11",
    "wrangler": "4.114.0"
  }
}
